//
// DeviceAuthnKey.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct DeviceAuthnKey: Codable, JSONEncodable, Hashable {

    /** ClientKeyID is a client-chosen id for the key and is unique per identity. */
    public var clientKeyId: String?
    /** CreatedAt is the timestamp of when the key was created. Only used for troubleshooting/UI. */
    public var createdAt: Date?
    /** DeviceName is a human readable name for the device, helping the user to distinguish it from others. */
    public var deviceName: String?
    public var deviceType: String?
    /** PublicKey is an EC public key, used to verify signatures, stored as uncompressed bytes. The private key resides inside the device and does not exist on the server. */
    public var publicKey: [Int]?

    public init(clientKeyId: String? = nil, createdAt: Date? = nil, deviceName: String? = nil, deviceType: String? = nil, publicKey: [Int]? = nil) {
        self.clientKeyId = clientKeyId
        self.createdAt = createdAt
        self.deviceName = deviceName
        self.deviceType = deviceType
        self.publicKey = publicKey
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clientKeyId = "client_key_id"
        case createdAt = "created_at"
        case deviceName = "device_name"
        case deviceType = "device_type"
        case publicKey = "public_key"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clientKeyId, forKey: .clientKeyId)
        try container.encodeIfPresent(createdAt, forKey: .createdAt)
        try container.encodeIfPresent(deviceName, forKey: .deviceName)
        try container.encodeIfPresent(deviceType, forKey: .deviceType)
        try container.encodeIfPresent(publicKey, forKey: .publicKey)
    }
}

