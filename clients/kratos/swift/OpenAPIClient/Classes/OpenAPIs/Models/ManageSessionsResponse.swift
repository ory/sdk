//
// ManageSessionsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Response body for the bulk session management endpoint. Reports how many rows the call processed and, for the wildcard variant, whether the network still has matching rows left. Explicit-IDs requests always return &#x60;more: false&#x60;. Wildcard callers drain the network by re-issuing the same request while &#x60;more&#x60; is &#x60;true&#x60;. */
public struct ManageSessionsResponse: Codable, JSONEncodable, Hashable {

    /** True when the call reached the per-call batch limit and additional matching rows may remain. Always false for explicit-IDs requests. */
    public var more: Bool?
    /** Number of sessions processed in this call. For `disable`, counts only sessions that were active before the call (already-inactive sessions are skipped). For `delete`, counts every matching row removed. */
    public var processed: Int64?

    public init(more: Bool? = nil, processed: Int64? = nil) {
        self.more = more
        self.processed = processed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case more
        case processed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(more, forKey: .more)
        try container.encodeIfPresent(processed, forKey: .processed)
    }
}

