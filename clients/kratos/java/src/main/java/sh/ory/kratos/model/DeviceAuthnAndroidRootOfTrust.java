/*
 * Ory Identities API
 * This is the API specification for Ory Identities with features such as registration, login, recovery, account verification, profile settings, password reset, identity management, session management, email and sms delivery, and more. 
 *
 * The version of the OpenAPI document: 
 * Contact: office@ory.sh
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package sh.ory.kratos.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import sh.ory.kratos.JSON;

/**
 * DeviceAuthnAndroidRootOfTrust
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-05-07T09:37:47.441414+02:00[Europe/Berlin]", comments = "Generator version: 7.12.0")
public class DeviceAuthnAndroidRootOfTrust {
  public static final String SERIALIZED_NAME_DEVICE_LOCKED = "device_locked";
  @SerializedName(SERIALIZED_NAME_DEVICE_LOCKED)
  @javax.annotation.Nullable
  private Boolean deviceLocked;

  public static final String SERIALIZED_NAME_VERIFIED_BOOT_HASH = "verified_boot_hash";
  @SerializedName(SERIALIZED_NAME_VERIFIED_BOOT_HASH)
  @javax.annotation.Nullable
  private List<Integer> verifiedBootHash = new ArrayList<>();

  public static final String SERIALIZED_NAME_VERIFIED_BOOT_KEY = "verified_boot_key";
  @SerializedName(SERIALIZED_NAME_VERIFIED_BOOT_KEY)
  @javax.annotation.Nullable
  private List<Integer> verifiedBootKey = new ArrayList<>();

  public static final String SERIALIZED_NAME_VERIFIED_BOOT_STATE = "verified_boot_state";
  @SerializedName(SERIALIZED_NAME_VERIFIED_BOOT_STATE)
  @javax.annotation.Nullable
  private Long verifiedBootState;

  public DeviceAuthnAndroidRootOfTrust() {
  }

  public DeviceAuthnAndroidRootOfTrust deviceLocked(@javax.annotation.Nullable Boolean deviceLocked) {
    this.deviceLocked = deviceLocked;
    return this;
  }

  /**
   * Get deviceLocked
   * @return deviceLocked
   */
  @javax.annotation.Nullable
  public Boolean getDeviceLocked() {
    return deviceLocked;
  }

  public void setDeviceLocked(@javax.annotation.Nullable Boolean deviceLocked) {
    this.deviceLocked = deviceLocked;
  }


  public DeviceAuthnAndroidRootOfTrust verifiedBootHash(@javax.annotation.Nullable List<Integer> verifiedBootHash) {
    this.verifiedBootHash = verifiedBootHash;
    return this;
  }

  public DeviceAuthnAndroidRootOfTrust addVerifiedBootHashItem(Integer verifiedBootHashItem) {
    if (this.verifiedBootHash == null) {
      this.verifiedBootHash = new ArrayList<>();
    }
    this.verifiedBootHash.add(verifiedBootHashItem);
    return this;
  }

  /**
   * Get verifiedBootHash
   * @return verifiedBootHash
   */
  @javax.annotation.Nullable
  public List<Integer> getVerifiedBootHash() {
    return verifiedBootHash;
  }

  public void setVerifiedBootHash(@javax.annotation.Nullable List<Integer> verifiedBootHash) {
    this.verifiedBootHash = verifiedBootHash;
  }


  public DeviceAuthnAndroidRootOfTrust verifiedBootKey(@javax.annotation.Nullable List<Integer> verifiedBootKey) {
    this.verifiedBootKey = verifiedBootKey;
    return this;
  }

  public DeviceAuthnAndroidRootOfTrust addVerifiedBootKeyItem(Integer verifiedBootKeyItem) {
    if (this.verifiedBootKey == null) {
      this.verifiedBootKey = new ArrayList<>();
    }
    this.verifiedBootKey.add(verifiedBootKeyItem);
    return this;
  }

  /**
   * Get verifiedBootKey
   * @return verifiedBootKey
   */
  @javax.annotation.Nullable
  public List<Integer> getVerifiedBootKey() {
    return verifiedBootKey;
  }

  public void setVerifiedBootKey(@javax.annotation.Nullable List<Integer> verifiedBootKey) {
    this.verifiedBootKey = verifiedBootKey;
  }


  public DeviceAuthnAndroidRootOfTrust verifiedBootState(@javax.annotation.Nullable Long verifiedBootState) {
    this.verifiedBootState = verifiedBootState;
    return this;
  }

  /**
   * Get verifiedBootState
   * @return verifiedBootState
   */
  @javax.annotation.Nullable
  public Long getVerifiedBootState() {
    return verifiedBootState;
  }

  public void setVerifiedBootState(@javax.annotation.Nullable Long verifiedBootState) {
    this.verifiedBootState = verifiedBootState;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the DeviceAuthnAndroidRootOfTrust instance itself
   */
  public DeviceAuthnAndroidRootOfTrust putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    DeviceAuthnAndroidRootOfTrust deviceAuthnAndroidRootOfTrust = (DeviceAuthnAndroidRootOfTrust) o;
    return Objects.equals(this.deviceLocked, deviceAuthnAndroidRootOfTrust.deviceLocked) &&
        Objects.equals(this.verifiedBootHash, deviceAuthnAndroidRootOfTrust.verifiedBootHash) &&
        Objects.equals(this.verifiedBootKey, deviceAuthnAndroidRootOfTrust.verifiedBootKey) &&
        Objects.equals(this.verifiedBootState, deviceAuthnAndroidRootOfTrust.verifiedBootState)&&
        Objects.equals(this.additionalProperties, deviceAuthnAndroidRootOfTrust.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(deviceLocked, verifiedBootHash, verifiedBootKey, verifiedBootState, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class DeviceAuthnAndroidRootOfTrust {\n");
    sb.append("    deviceLocked: ").append(toIndentedString(deviceLocked)).append("\n");
    sb.append("    verifiedBootHash: ").append(toIndentedString(verifiedBootHash)).append("\n");
    sb.append("    verifiedBootKey: ").append(toIndentedString(verifiedBootKey)).append("\n");
    sb.append("    verifiedBootState: ").append(toIndentedString(verifiedBootState)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("device_locked");
    openapiFields.add("verified_boot_hash");
    openapiFields.add("verified_boot_key");
    openapiFields.add("verified_boot_state");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to DeviceAuthnAndroidRootOfTrust
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!DeviceAuthnAndroidRootOfTrust.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in DeviceAuthnAndroidRootOfTrust is not found in the empty JSON string", DeviceAuthnAndroidRootOfTrust.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("verified_boot_hash") != null && !jsonObj.get("verified_boot_hash").isJsonNull() && !jsonObj.get("verified_boot_hash").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `verified_boot_hash` to be an array in the JSON string but got `%s`", jsonObj.get("verified_boot_hash").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("verified_boot_key") != null && !jsonObj.get("verified_boot_key").isJsonNull() && !jsonObj.get("verified_boot_key").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `verified_boot_key` to be an array in the JSON string but got `%s`", jsonObj.get("verified_boot_key").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!DeviceAuthnAndroidRootOfTrust.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'DeviceAuthnAndroidRootOfTrust' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<DeviceAuthnAndroidRootOfTrust> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(DeviceAuthnAndroidRootOfTrust.class));

       return (TypeAdapter<T>) new TypeAdapter<DeviceAuthnAndroidRootOfTrust>() {
           @Override
           public void write(JsonWriter out, DeviceAuthnAndroidRootOfTrust value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public DeviceAuthnAndroidRootOfTrust read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             DeviceAuthnAndroidRootOfTrust instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of DeviceAuthnAndroidRootOfTrust given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of DeviceAuthnAndroidRootOfTrust
   * @throws IOException if the JSON string is invalid with respect to DeviceAuthnAndroidRootOfTrust
   */
  public static DeviceAuthnAndroidRootOfTrust fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, DeviceAuthnAndroidRootOfTrust.class);
  }

  /**
   * Convert an instance of DeviceAuthnAndroidRootOfTrust to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

