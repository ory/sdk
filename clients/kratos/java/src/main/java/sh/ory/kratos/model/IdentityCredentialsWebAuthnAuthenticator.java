/*
 * Ory Identities API
 * This is the API specification for Ory Identities with features such as registration, login, recovery, account verification, profile settings, password reset, identity management, session management, email and sms delivery, and more. 
 *
 * The version of the OpenAPI document: 
 * Contact: office@ory.sh
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package sh.ory.kratos.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import sh.ory.kratos.JSON;

/**
 * IdentityCredentialsWebAuthnAuthenticator
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-05-07T09:37:47.441414+02:00[Europe/Berlin]", comments = "Generator version: 7.12.0")
public class IdentityCredentialsWebAuthnAuthenticator {
  public static final String SERIALIZED_NAME_AAGUID = "aaguid";
  @SerializedName(SERIALIZED_NAME_AAGUID)
  @javax.annotation.Nullable
  private List<Integer> aaguid = new ArrayList<>();

  public static final String SERIALIZED_NAME_CLONE_WARNING = "clone_warning";
  @SerializedName(SERIALIZED_NAME_CLONE_WARNING)
  @javax.annotation.Nullable
  private Boolean cloneWarning;

  public static final String SERIALIZED_NAME_SIGN_COUNT = "sign_count";
  @SerializedName(SERIALIZED_NAME_SIGN_COUNT)
  @javax.annotation.Nullable
  private Integer signCount;

  public IdentityCredentialsWebAuthnAuthenticator() {
  }

  public IdentityCredentialsWebAuthnAuthenticator aaguid(@javax.annotation.Nullable List<Integer> aaguid) {
    this.aaguid = aaguid;
    return this;
  }

  public IdentityCredentialsWebAuthnAuthenticator addAaguidItem(Integer aaguidItem) {
    if (this.aaguid == null) {
      this.aaguid = new ArrayList<>();
    }
    this.aaguid.add(aaguidItem);
    return this;
  }

  /**
   * Get aaguid
   * @return aaguid
   */
  @javax.annotation.Nullable
  public List<Integer> getAaguid() {
    return aaguid;
  }

  public void setAaguid(@javax.annotation.Nullable List<Integer> aaguid) {
    this.aaguid = aaguid;
  }


  public IdentityCredentialsWebAuthnAuthenticator cloneWarning(@javax.annotation.Nullable Boolean cloneWarning) {
    this.cloneWarning = cloneWarning;
    return this;
  }

  /**
   * Get cloneWarning
   * @return cloneWarning
   */
  @javax.annotation.Nullable
  public Boolean getCloneWarning() {
    return cloneWarning;
  }

  public void setCloneWarning(@javax.annotation.Nullable Boolean cloneWarning) {
    this.cloneWarning = cloneWarning;
  }


  public IdentityCredentialsWebAuthnAuthenticator signCount(@javax.annotation.Nullable Integer signCount) {
    this.signCount = signCount;
    return this;
  }

  /**
   * Get signCount
   * @return signCount
   */
  @javax.annotation.Nullable
  public Integer getSignCount() {
    return signCount;
  }

  public void setSignCount(@javax.annotation.Nullable Integer signCount) {
    this.signCount = signCount;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the IdentityCredentialsWebAuthnAuthenticator instance itself
   */
  public IdentityCredentialsWebAuthnAuthenticator putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IdentityCredentialsWebAuthnAuthenticator identityCredentialsWebAuthnAuthenticator = (IdentityCredentialsWebAuthnAuthenticator) o;
    return Objects.equals(this.aaguid, identityCredentialsWebAuthnAuthenticator.aaguid) &&
        Objects.equals(this.cloneWarning, identityCredentialsWebAuthnAuthenticator.cloneWarning) &&
        Objects.equals(this.signCount, identityCredentialsWebAuthnAuthenticator.signCount)&&
        Objects.equals(this.additionalProperties, identityCredentialsWebAuthnAuthenticator.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(aaguid, cloneWarning, signCount, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class IdentityCredentialsWebAuthnAuthenticator {\n");
    sb.append("    aaguid: ").append(toIndentedString(aaguid)).append("\n");
    sb.append("    cloneWarning: ").append(toIndentedString(cloneWarning)).append("\n");
    sb.append("    signCount: ").append(toIndentedString(signCount)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("aaguid");
    openapiFields.add("clone_warning");
    openapiFields.add("sign_count");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to IdentityCredentialsWebAuthnAuthenticator
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!IdentityCredentialsWebAuthnAuthenticator.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in IdentityCredentialsWebAuthnAuthenticator is not found in the empty JSON string", IdentityCredentialsWebAuthnAuthenticator.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("aaguid") != null && !jsonObj.get("aaguid").isJsonNull() && !jsonObj.get("aaguid").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `aaguid` to be an array in the JSON string but got `%s`", jsonObj.get("aaguid").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!IdentityCredentialsWebAuthnAuthenticator.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'IdentityCredentialsWebAuthnAuthenticator' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<IdentityCredentialsWebAuthnAuthenticator> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(IdentityCredentialsWebAuthnAuthenticator.class));

       return (TypeAdapter<T>) new TypeAdapter<IdentityCredentialsWebAuthnAuthenticator>() {
           @Override
           public void write(JsonWriter out, IdentityCredentialsWebAuthnAuthenticator value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public IdentityCredentialsWebAuthnAuthenticator read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             IdentityCredentialsWebAuthnAuthenticator instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of IdentityCredentialsWebAuthnAuthenticator given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of IdentityCredentialsWebAuthnAuthenticator
   * @throws IOException if the JSON string is invalid with respect to IdentityCredentialsWebAuthnAuthenticator
   */
  public static IdentityCredentialsWebAuthnAuthenticator fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, IdentityCredentialsWebAuthnAuthenticator.class);
  }

  /**
   * Convert an instance of IdentityCredentialsWebAuthnAuthenticator to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

