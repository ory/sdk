# OryClient::OnboardingPortalTestLoginFlow

## Properties

| Name | Type | Description | Notes |
| ---- | ---- | ----------- | ----- |
| **id** | **String** | ID of the created test login flow. Append this as the &#x60;flow&#x60; query parameter on the test sign-in UI URL. |  |

## Example

```ruby
require 'ory-client'

instance = OryClient::OnboardingPortalTestLoginFlow.new(
  id: null
)
```

