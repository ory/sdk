version: 2.1
orbs:
  changelog: ory/changelog@0.0.3
  sdk: ory/sdk@0.0.4

jobs:
  publish:
    docker:
      - image: oryd/sdk:v0.0.2
    working_directory: /sdk
    steps:
      - checkout
      - run: ./scripts/init.sh
      - run: ./scripts/generate.sh
      - run: ./scripts/release.sh

workflows:
  publish:
    jobs:
      - publish
